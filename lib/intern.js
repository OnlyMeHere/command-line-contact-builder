// Constructor function called to extend Employee Class to Intern
// adding school.

class Intern extends Employee {

    constructor(name, id, email, school) {

        this.name = nameInt;
        this.id = idInt;
        this.email = emailInt;
        this.school = school;
        this.getname = () => {
            console.log(`name: ${this.nameInt}`);
        };
        this.getId = () => {
            console.log(`ID: ${this.idInt}`);
        };
        this.getEmail = () => {
            console.log(`Email: ${this.emailInt}`);
        };
        this.getSchool = () => {
            console.log(`school: ${school}`)
        };
    
    };

};

module.exports - Intern